{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "testing socket io",
  "scripts": {
    "dev": "node index.js",
    "format:fix": "prettier --config .prettierrc . --write"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.5.3"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
